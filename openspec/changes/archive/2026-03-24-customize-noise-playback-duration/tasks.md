## 1. 配置与界面建模

- [x] 1.1 在 `ScenarioConfig`、配置加载和保存流程中新增 `noise_playback_duration_ms`，并保持旧配置默认兼容
- [x] 1.2 更新 GUI 场景表，使操作者可以查看和编辑每个场景的噪声播放时长
- [x] 1.3 更新 `sample_config.yaml` 和预检快照展示逻辑，确保最终生效的噪声播放时长可见

## 2. 运行时播放控制

- [x] 2.1 在场景执行流程中为噪声播放记录开始时间和可选停止截止时间
- [x] 2.2 在预热、试次等待和试次间隔阶段接入噪声自动停止检查，达到自定义时长后停止噪声而不中断剩余试次
- [x] 2.3 保持默认行为兼容，未配置或配置为 `0` 时继续按整场景播放噪声

## 3. 回归验证与文档

- [x] 3.1 为配置兼容、预检展示和运行时噪声停止行为补充自动化测试
- [x] 3.2 更新 README 和中文使用文档，说明噪声播放时长的配置方式与默认语义
- [x] 3.3 手动验证 GUI 配置、自定义时长生效、默认兼容和运行报告输出流程
