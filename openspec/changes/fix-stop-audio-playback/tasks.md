## 1. 音频句柄中断能力

- [x] 1.1 为单次播放句柄补充统一的 `stop()` 语义，并让真实后端与 dry-run 后端都提供幂等实现
- [x] 1.2 调整 `sounddevice` 单次播放与循环播放的回调/收尾逻辑，确保用户停止后不再继续送样

## 2. 引擎停止链路

- [x] 2.1 让引擎显式跟踪当前活动的唤醒词播放句柄与噪声句柄，并在 `request_stop()` 中立即停止它们
- [x] 2.2 调整正式运行中的等待与收尾逻辑，确保用户停止后当前试次尽快结束且后续试次不再启动
- [x] 2.3 调整试听逻辑，使试听任务也能响应停止请求而不是始终阻塞到播放结束

## 3. 界面反馈与回归测试

- [x] 3.1 优化 GUI 停止状态反馈，确保停止中与停止完成的提示一致且不误导
- [x] 3.2 补充运行模式下“停止后唤醒词/噪声被中断”的自动化测试
- [x] 3.3 补充试听模式下“停止后立即停声”的自动化测试
- [x] 3.4 补充重复停止与资源回收的回归测试，防止幂等性退化
